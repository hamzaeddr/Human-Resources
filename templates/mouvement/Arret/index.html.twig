{% extends 'base.html.twig' %}

{% block title %}RH - Gestion des echeances{% endblock %}


{% block body %}
       <div>
        <div class="col-12 mb-4">
            <div class="card  border-0 shadow">
                <div class="card-body p-2 m-2">
                    <div class="row">
    {% include 'includes/actions.html.twig' %}
                       
                    </div>
                    <hr>
                  
                    <table id="parret_datatable" class="table table-striped table-bordered" style="width:100%">

    <thead>
        <tr>
            <th>Id Arret</th>
            <th>ID_Employe</th>
            <th>Matricule</th>
            <th>Nom</th>
            <th>Prenom</th>
            <th>Motif</th>
            <th>Date debut</th>
            <th>Date Fin</th>
            <th>Date reprise</th>
            <th>Details</th>
        </tr>
    </thead>
    <tbody>
        {# {% for result in results %}

      <tr>
                        <td>{{result.arret_id}}</td>
                        <td>{{result.employe_id}}</td>
                        <td>{{result.matricule}}</td>
                        <td>{{result.motif}}</td>
                        <td>{{ result.date_debut|date('Y-m-d H:i:s') }}</td>    

                        <td>{{ result.date_fin|date('Y-m-d H:i:s') }}</td>
                        <td>{{ result.date_rep|date('Y-m-d H:i:s') }}</td>
                        <td><i class='fa fa-eye text-info arretlgdetails' role="button" data-id="{{result.arret_id}}"></td>
                       
                </tr>
        {% endfor %} #}
      


    </tbody>
</table>

                </div>
            </div>
        </div>
    </div>
    
    {% include 'mouvement/Arret/modals/ajouter_arret.html.twig' %} 
    {% include 'mouvement/Arret/modals/ajouter_arret.html.twig' %}   
    {# {% include 'mouvement/Arret/includes/arret_detail.html.twig' %} #}
    {% include 'mouvement/Arret/modals/arret_details.html.twig' %}


{% endblock %}


{% block javascripts %}
    {{parent()}}
    {{ encore_entry_script_tags('mouvement_arret') }}
{% endblock %}

